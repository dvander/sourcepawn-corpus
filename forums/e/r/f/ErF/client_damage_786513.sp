/* Plugin Template generated by Pawn Studio */

#include <sourcemod>
new damage[33];
public Plugin:myinfo = 
{
	name = "Damage Sum",
	author = "ErF",
	description = "/obrazenia to view damage sum",
	version = "1.0",
	url = "www.pgc-cs.eu"
}

public OnPluginStart()
{
	RegConsoleCmd("obrazenia",Obrazenia);
	HookEvent("player_hurt",Event_Damage);
}
public OnPlayerConnect(client)
{
	damage[client] = 0;
}
public Event_Damage(Handle:event, const String:name[], bool:dontBroadcast)
{
	new attacker = GetClientOfUserId(GetEventInt(event,"attacker"));
	new dmg = GetEventInt(event,"dmg_health ");
	damage[attacker] += dmg;
}
public Action:Obrazenia(client,args)
{
	PrintToChat(client,"Podczas pobytu na serwerze ³¹cznie zada³eœ: %d obra¿eñ.",damage[client]);
	return Plugin_Handled;
}