/* Script generated by SourcePawn IDE */

#include <sourcemod>

public Plugin:myinfo =
{
    name = "Vote with Chat Commands!",
    author = "cREANy0",
    description = "Allow People to vote with Chat Commands",
    version = "1.0",
    url = "http://sourcemod.net"
}

public OnPluginStart()
{
    RegConsoleCmd("sm_v1",Vote1);
    RegConsoleCmd("sm_v2",Vote2);
    RegConsoleCmd("sm_v3",Vote3);
    RegConsoleCmd("sm_v4",Vote4);
    RegConsoleCmd("sm_v5",Vote5);
    RegConsoleCmd("sm_v6",Vote6);
    RegConsoleCmd("sm_v7",Vote7);
    RegConsoleCmd("sm_v8",Vote8);
    RegConsoleCmd("sm_v9",Vote9);    
    RegConsoleCmd("sm_v0",Vote0);
}


public Action:Vote1(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 1") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote2(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 2") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote3(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 3") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote4(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 4") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote5(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 5") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote6(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 6") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote7(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 7") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote8(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 8") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote9(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 9") // execute menuselect in the client for choose a option in the vote
}

public Action:Vote0(client, args)
{
   FakeClientCommand(client, "sm_revote") // open vote menu
   FakeClientCommand(client, "menuselect 0") // execute menuselect in the client for choose a option in the vote
}

