/* Plugin Template generated by Pawn Studio */

#include <sourcemod>
#undef REQUIRE_PLUGIN
#include "include\sodsettings.inc"

public Plugin:myinfo = 
{
	name = "SoD Settings Example 2",
	author = "]SoD[ Frostbyte",
	description = "Second example use of sodsettings natives and forwards.",
	version = "1.0",
	url = "www.sonsofdavid.net"
}

public OnPluginStart()
{
}

public OnSettingsChanged(Handle:menu, MenuAction:action, param1, param2)
{
	new String:info[32];
	GetMenuItem(menu, param2, info, sizeof(info));
	if(strcmp(info, "displaysettings") == 0)
	{
		Settings_DisplayMenu(param1);
		PrintToChat(param1, "\x04[SoD-Settings] Displaying settings menu");
	}
}

public OnDisplaySettings(client, Handle:menu)
{
	AddMenuItem(menu, "displaysettings", "Display settings");
}
